


**VITE CONFIG FILE FROM FRONTEND**
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  build: {
    manifest: true,
    rollupOptions: {
      input: "./src/main.jsx",
    },
  },
});

• npm i express cors body-parser pg axios css-loader file-loader react-router-dom sass sass-loader style-loader
• npm install -D concurrently nodemon
• npm i react-redux react-scripts redux
• npm i bootstrap react-bootstrap
• npm i react-icons

• Ember made a copy of this morning's dev code,
  git checkout -b EmbeRefresh
  cleared everything from the directory,
  npm init -y
  npm create vite@latest
    input "project name" as couchPotato,
      (this is temporary)
    accepted "package name" as couchPotato,
    selected "React" for "framework",
    selected "JavaScript" for "variant",
  remove package.json,
  moved everything inside couchPotato out a level into root,
    (unnesting everything)
  removed empty couchPotato,
  npm install
  npm i concurrently
  npm run dev
    confirmed on http://localhost:5173/

  * stuff in public folder is unnecessary
  * if issues, try removing package-lock.json




